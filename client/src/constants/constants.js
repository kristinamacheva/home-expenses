// IMPORTANT: If the server is running on a different IP address or port, 
// update this value accordingly to match your server's configuration.

// Base URL for the API
export const API_BASE_URL = "http://localhost:5000";

